<div class="hero">
  <div class="hero-inner">
    <a href "" class="hero-logo"></a>
    <img src="https://raw.githubusercontent.com/thoughtbot/refills/master/source/images/placeholder_logo_1.png" alt="Logo Image">
    <div class="hero-copy">
      <h1>The Wealth Gap Slider</h1>
      <p>See where minimum wages would be if the poverty gap remained consistent at 1979 levels</p>
    </div>
    <button>Learn More</button>
  </div>
</div>


<div class="companies-container">
  <div class="companies-inner">
    <% @companies.each do |company| %>
    <div class="company">
      <img src="<%= company[:image] %>">
      <p>Each year, <%= company[:name] %> earns a profit of $5,000,000,000.  The company currently employs <%= company[:employees] %> workers at an average wage of <%= company[:average] %> and a minumum wage of <%= company[:minumum] %>.  The company CEO earns $<%= company[:ceo_salary] %>.</p>
      <div class="slider-container">
        <div class="slider">
          CEO Salary
          <input type = "range" id="slider" min="0" max="100" value="100" step=".5" onchange="rangevalue.value=value"/>
          <span class="wage-zero">$0</span>
          <output id="rangevalue">100</output>
          <span id="ceo-salary" class="ceo-salary">$<%= company[:ceo_salary] %></span>
          </div>
        </div>
    </div>
    <% end %>
  </div>
</div>
